# Abraham TUI

Operator console built with `ratatui`.

- Session list with metadata from `REGISTER`
- Task submission (SHELL, UPLOAD, DOWNLOAD, SLEEP, EXIT)
- Streaming result view (CHUNK-aware)

Status: Phase 1 core implemented (sessions, task submission, output view).
